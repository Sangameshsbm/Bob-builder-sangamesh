# ============================================
# WATSONX HACKATHON - ENVIRONMENT VARIABLES
# ============================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env (which is in .gitignore)
#    Command: cp .env.example .env
# 2. Replace placeholder values with your actual credentials
# 3. NEVER commit the .env file to git
# 4. NEVER share your credentials in code or with AI assistants
#
# ============================================

# IBM Cloud API Key
# Get from: https://cloud.ibm.com/iam/apikeys
IBM_CLOUD_API_KEY=your_api_key_here_DO_NOT_COMMIT


# ============================================
# SECURITY REMINDERS:
# - This .env.example file is safe to commit
# - Your actual .env file is in .gitignore
# - Never hardcode credentials in your code
# - Use process.env.VARIABLE_NAME to access values
# - Never share credentials with AI assistants like Bob
# ============================================
# ============================================
# CODE FOR IBM i DETECTIVE
# ============================================

# Mock mode - set true for offline demo (no IBM i or GitHub required)
IBM_I_MOCK=true

# Mock scenario to load when IBM_I_MOCK=true
# Values: mapepire-hang (tier-2 main act), port449 (tier-1 warm-up)
IBM_I_MOCK_SCENARIO=mapepire-hang

# GitHub token for live GitHub issues search (recommended, not required)
# Get from: https://github.com/settings/tokens - needs public_repo read scope only
GITHUB_TOKEN=your_github_token_here_DO_NOT_COMMIT

# VS Code extension version (used in live mode diagnostics)
CODEFORI_VERSION=3.0.2
VSCODE_VERSION=1.96.0
